<html>

<head>
    <style>
        .search-box {
            position: fixed;
            z-index: 100;
            transform: translate(0%, 0);
            background: #4CAF50;
            height: 40px;
            border-radius: 40px;
            margin-left: 20px;
        }

     .search-box:hover>.search-text {
            width: 240px;
            padding:0 12px;
        }

        .search-box:hover>.search-btn {
            background: white;
        }

        .search-box:hover>.fa {
            color: red;
        }

        .search-btn {
            color: white;
            float: right;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: #4CAF50;
            display: flex;
            justify-content: center;
            align-items: center;
            transition: 0.4s;
        }

        .search-text {
            border: none;
            background: none;
            outline: none;
            float: left;
            padding: 0;
            color: white;
            font-size: 16px;
            transition: 0.4s;
            line-height: 40px;
            width: 0px;
        }
    </style>
    <meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>

<body>
    <form action="/search" method="POST">
    <div class="search-box">
        <input class="search-text" name="searchText" type="text" name="" placeholder="Type to search">
        <span class="search-btn">
                <i class="fa fa-search"></i></span>
</form>

    </div>

</body>

</html>